# ============================================================
# 🔐 보안: API 키 / 비밀번호 / 환경설정
# ============================================================
.env
.env.*
!.env.example
.bai-feed.env
.env.local
.env.production
*.key
*.pem
*.p12
*.pfx
*secret*
*credential*
config.yaml
config.yml
secrets.json
20-areas/kakaotalk/config.json
20-areas/kakaotalk/kakao_token.json

# 도구별 로컬 설정 (개인 인증 정보 포함 가능)
.codex/local.*
.codex/local-tools/
.codex/*.local.json
.codex/local-python.json
.codex/tmp/
.python-path

# 참고: .codex/config.toml 은 의도적으로 커밋합니다 (MCP 서버 등록만 담는 파일).
# 환경변수 키(API 키 등)는 절대 config.toml 에 직접 적지 마세요.
# 실제 비밀값은 .env 또는 .codex/local.* 에만 두고, 둘 다 위에서 무시됩니다.

# ============================================================
# Python
# ============================================================
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
*.ipynb_checkpoints/

# ============================================================
# Node.js
# ============================================================
node_modules/
npm-debug.log*
yarn-error.log*
package-lock.json

# ============================================================
# VSCode
# ============================================================
.vscode/
!.vscode/extensions.json
*.code-workspace

# ============================================================
# OS 파일
# ============================================================
.DS_Store
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# ============================================================
# 데이터 파일 (개인정보 포함 가능성 있는 폴더만 제외)
# ============================================================
data/raw/
data/private/

# ============================================================
# 로그 / 임시 파일
# ============================================================
.setup-done
*.log
*.tmp
*.temp
.cache/

# ============================================================
# 별도 GitHub 저장소로 독립 배포되는 하위 프로젝트
# (자체 .git을 가지므로 메인 워크스페이스 저장소에서는 추적하지 않음)
# ============================================================
10-projects/incheon-univ-notices/
10-projects/kr-emcare/
10-projects/dotfiles/
10-projects/bai-shared/
40-archive/kr-emcare/
40-archive/china-trend-tracker/
20-areas/GitHub Codespaces용 개인 셋업 저장소/

# ============================================================
# 다운로드되는 개발 도구 (소스가 아니라 설치물이라 커밋 대상 아님)
# ============================================================
AndroidSdk/
