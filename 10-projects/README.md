# 10-projects

지금 만들고 있는 작업을 저장하는 폴더입니다.

새 작업을 시작하면 Codex가 `작업이름/` 폴더를 만들고, 그 안에 `README.md`, `src/`, `input/`, `output/`, `notes/`를 구성합니다.

참고 예시는 `30-resources/examples/`에 모아 둡니다. 새 작업은 `10-projects/작업이름/`에 만듭니다.
